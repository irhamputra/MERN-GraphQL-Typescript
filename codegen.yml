overwrite: true
schema: "http://localhost:4000"
documents: null
generates:
  server/src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
      - "typescript-mongodb"
